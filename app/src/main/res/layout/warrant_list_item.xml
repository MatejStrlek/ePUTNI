<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/item_background"
    android:layout_margin="@dimen/_10sp"
    android:orientation="horizontal"
    android:padding="@dimen/_30sp">

    <ImageView
        android:layout_width="@dimen/_50sp"
        android:layout_height="@dimen/_50sp"
        android:layout_marginEnd="16dp"
        android:src="@drawable/ic_warrant"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvWarrantDestination"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="@string/test_warrant_name"
            android:textSize="@dimen/_18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvWarrantFinishedDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="@string/test_time_finished"
            android:textSize="@dimen/_14sp" />

    </LinearLayout>

    <ImageView
        android:id="@+id/ivWarrantChecked"
        android:layout_width="@dimen/_50sp"
        android:layout_height="@dimen/_50sp"
        android:layout_marginEnd="@dimen/_8sp"
        tools:src="@drawable/ic_checked_yes"
        android:importantForAccessibility="no" />

</LinearLayout>